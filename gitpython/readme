#basics of gipython module will be mentioned in the python scripts in gitpython  folder
import git #imports the modules


repo_url=""  #provide the repo url of the remote repo
local_path= "" #provide the path where you want to  checkout
#provide the name in the local path for hte name  of the local repo

#this will clone the repo
git.Repo.clone_from(repo_url,local_path)
